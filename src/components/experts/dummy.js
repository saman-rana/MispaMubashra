import React, {useState} from 'react';
import {
  View,
  Text,
  Pressable,
  StyleSheet,
  TouchableOpacity,
} from 'react-native';

const BottomSheetOpener = ({item}) => {
  return <></>;
};

const Styles = StyleSheet.create({});

export default BottomSheetOpener;
